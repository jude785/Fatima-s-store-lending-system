<nav class="navbar navbar-expand-lg app-navbar">
  <div class="container">
    <a class="navbar-brand" href="/dashboard">
      <span class="brand-mark"><i class="bi bi-shop-window"></i></span>
      <span>
        <span class="brand-title">FATIMA'S STORE</span>
        <span class="brand-subtitle">Lending System</span>
      </span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navMenu">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><a class="nav-link <%= currentPath.startsWith('/dashboard') ? 'active' : '' %>" href="/dashboard"><i class="bi bi-grid-1x2"></i>Dashboard</a></li>
        <li class="nav-item"><a class="nav-link <%= currentPath.startsWith('/borrowers') ? 'active' : '' %>" href="/borrowers"><i class="bi bi-people"></i>Borrowers</a></li>
        <li class="nav-item"><a class="nav-link <%= currentPath.startsWith('/loans') ? 'active' : '' %>" href="/loans"><i class="bi bi-cash-stack"></i>Loans</a></li>
        <li class="nav-item"><a class="nav-link <%= currentPath.startsWith('/payments') ? 'active' : '' %>" href="/payments"><i class="bi bi-wallet2"></i>Payments</a></li>
        <li class="nav-item"><a class="nav-link <%= currentPath.startsWith('/reports') ? 'active' : '' %>" href="/reports"><i class="bi bi-bar-chart-line"></i>Reports</a></li>
        <% if (currentUser.role === 'Administrator') { %>
          <li class="nav-item"><a class="nav-link <%= currentPath.startsWith('/users') ? 'active' : '' %>" href="/users"><i class="bi bi-shield-lock"></i>Users</a></li>
          <li class="nav-item"><a class="nav-link <%= currentPath.startsWith('/audit-logs') ? 'active' : '' %>" href="/audit-logs"><i class="bi bi-clock-history"></i>Audit</a></li>
        <% } %>
      </ul>
      <div class="navbar-account">
        <div class="user-chip">
          <span class="user-avatar"><%= (currentUser.full_name || currentUser.username || 'U').charAt(0).toUpperCase() %></span>
          <span>
            <span class="user-label">Signed in</span>
            <strong><%= currentUser.full_name || currentUser.username %></strong>
          </span>
        </div>
        <form action="/logout" method="POST" class="m-0">
          <%- csrfField %>
          <button class="btn btn-outline-light btn-sm" type="submit"><i class="bi bi-box-arrow-right me-1"></i>Logout</button>
        </form>
      </div>
    </div>
  </div>
</nav>
